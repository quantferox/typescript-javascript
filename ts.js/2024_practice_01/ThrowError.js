/* const checkAge = (age) => {
    if (age < 18) {
        throw new Error("Возраст должен быть 18 или старше");
    }
    return "Доступ разрешён";
}

const age = 17;

try {
    const isAge = checkAge(age);
    console.log(isAge);
}
catch (e) {
    console.log(e.message);
}
finally {
    console.log("Проверка завершена");
} */