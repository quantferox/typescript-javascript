/* function getPrice() {
    let req = new XMLHttpRequest();
    req.open("GET", "https://api.binance.com/api/v3/ticker/price?symbol=DOGEUSDT", false);
    req.send(undefined);
    postMessage(req.responseText);
    setTimeout(getPrice,2048);
}

getPrice(); */
