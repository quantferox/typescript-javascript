//Class Inheritance

/* class Turbo {
    constructor(engineVolume, engineType, engineHp) {
        this.engineVolume = engineVolume;
        this.engineType = engineType;
        this.engineHp = engineHp;
    }
    getInfoEngine() {
        return `${this.engineVolume} ${this.engineType} ${this.engineHp}`;
    }
}

class Car extends Turbo {
    constructor(brand, model, year, engineVolume, engineType, engineHp) {
        super(engineVolume, engineType, engineHp);
        this.brand = brand;
        this.model = model;
        this.year = year;
    }

    get zbrand() {
        return this.brand + "get";
    }

    set zbrand(x) {
        return this.brand + "x sett";
    }

    static getInfoCar() {
        return `${this.brand} ${this.model} ${this.year}`;
    }
}

const mercedes = new Car("Mercedes-Benz", "S450", "2023", "5.0", "V8", "542");

console.log(Car.getInfoCar()); */