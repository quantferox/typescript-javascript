//MAP

//Creates a new Map object

/* let mapValue = new Map([
    ["brand","Mercedes-Benz"],
    ["model","S500 Mansory W223"],
    ["price","325000$"],
]); */

// Creates a new Map object
/* let mapValue = new Map();
mapValue.set("brand", "Mercedes-Benz"); //Sets the value for a key in a Map
mapValue.set("model", "S500 Mansory W223"); // Sets the value for a key in a Map
mapValue.set("price", "325000$"); //Sets the value for a key in a Map */

/* console.log(mapValue.get("brand")); //Gets the value for a key in a Map
console.log(mapValue.get("model")); //Gets the value for a key in a Map
console.log(mapValue.get("price")); //Gets the value for a key in a Map */

/* mapValue.clear(); //Removes all the elements from a Map */
/* mapValue.delete("brand"); //Removes a Map element specified by a key */

/* console.log(mapValue.has("brand")); //Returns true if a key exists in a Map */

/* mapValue.forEach((v,k,m)=>{
    console.log(v); // Map value
    console.log(k); // Map key
    console.log(m); // Return map object
}); */

/* console.log(mapValue.entries()); // Returns an iterator object with the [key, value] pairs in a Map
console.log(mapValue.keys()); // Returns an iterator object with the keys in a Map
console.log(mapValue.values()); // Returns an iterator object of the values in a Map */

/* console.log(mapValue.size); // Returns the number of Map elements */

/* let mapValue = new Map([
    ["brand","Mercedes-Benz"],
    ["model","S500 Mansory W223"],
    ["price","325000$"],
]);

mapValue.set("brand","BMW");
mapValue.set("model","M5 F90 Hamann");
mapValue.set("price","225000$");

console.log(mapValue); */

/* let mapValue = new Map([
    ["brand","Mercedes-Benz"],
    ["model","S500 Mansory W223"],
    ["price","325000$"],
]);

console.log(typeof mapValue); // Object
console.log(mapValue instanceof Map); // true
console.log(mapValue instanceof Object); // true */

/* let obj_1 = {attackMethod: "Bruteforce"};
let obj_2 = {attackType: "Slow POST"};
let obj_3 = {attackSpeed: "7.9 Tbit/s"};

let mapValue = new Map();
mapValue.set(obj_1,true);
mapValue.set(obj_2,true);
mapValue.set(obj_3,true); */

/* console.log(mapValue); */