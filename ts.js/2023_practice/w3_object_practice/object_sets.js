//OBJECT SETS

/* let uniqueValue = new Set();

uniqueValue.add(1); // Adds a new element to the Set (1)
uniqueValue.add(3); // Adds a new element to the Set (3)
uniqueValue.add(2); // Adds a new element to the Set (2)
uniqueValue.add(1); // No Adds 1 (has(1) == true)

uniqueValue.delete(1); // Removes an element from a Set (1)
uniqueValue.clear(); // Removes all elements from a Set
uniqueValue.has(3); // Returns true if a value exists (3)

uniqueValue.forEach((cbKey, cbVal, cbSet) => {
    cbKey == cbVal // Because there are no keys in Sets
    cbSet // Return Sets object
});

uniqueValue.values(); // Return Sets Values iterator
uniqueValue.keys(); // Return Sets Values iterator - Because there are no keys in Sets 
uniqueValue.entries(); //Return keys => value (Because there are no keys in Sets) return value => value
uniqueValue.size; // Returns the number elements in a Set

console.log(typeof uniqueValue); //Return object
console.log(uniqueValue instanceof Set, uniqueValue instanceof Object); // Return true, true; */